baseURL = 'https://travel-journal-9lj.pages.dev/'
locale = 'en-us'
title = "Phil's Travel Journal"
theme = "PaperMod"
[markup]
  [markup.goldmark]
    [markup.goldmark.renderHooks]
      [markup.goldmark.renderHooks.image]
        useEmbedded = "fallback"
[params]
  assets = { css = ["css/extended.css"] }
[params.homeInfoParams]
  Title = "Willkommen zu meinem Reisetagebuch!"
  Content = "Klicke auf die unteren Posts um eine Station meiner Reise genauer zu erkunden."
